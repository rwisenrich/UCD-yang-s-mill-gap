# v8 transfer/source audit

## Correct transfer interlacing

An RG intertwiner `T'J ≈ JT` controls the full next-scale spectral edge only when the vacuum-orthogonal target space is uniformly approximated by `Ran J`. With range-density defect `delta` and intertwining defect `epsilon`,

`lambda2(T') <= lambda2(T) + epsilon + 2 delta + delta^2`.

## Correct continuum mass normalization

For `T_a = exp(-a H_a)`,

`m_a = -log(lambda2(T_a))/a`.

A finite positive continuum mass implies `1-lambda2(T_a) ~ a m`, so a fixed raw transfer gap is not the correct continuum invariant.

## Local curvature source scaling

If `||O_a|| <= C a^-d`, the elementary bounded-source zero-free disc guarantees only `|z| < (log 2) a^d/C`. For local `F^2` in four dimensions this is `O(a^4)` and collapses. The local-field source theorem therefore genuinely needs renormalized RG/cumulant control.

## Remaining load-bearing estimates

The proof dependency remains exactly `E1 + E2`, now with the physical generator-gap normalization explicit. No external paper is imported as a discharge unless its load-bearing uniform estimates are actually established.
